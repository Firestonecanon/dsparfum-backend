# Configuration locale pour le développement
NODE_ENV=development
PORT=3001

# Base de données locale (optionnel - utilise la production par défaut)
# DATABASE_URL=postgresql://localhost:5432/dsparfum_dev

# Laisser vide pour utiliser la base de production avec SSL
# (plus simple pour le développement car pas besoin d'installer PostgreSQL localement)
